// @ionexflow/ui — shared component library placeholder.
//
// This package intentionally ships empty in Phase 1 (monorepo/auth/schema).
// Phase 3 ("Web App Dashboard & React Flow Canvas editor") introduces the
// shadcn/ui-based component set here so both `apps/web` and any future
// surfaces can share the same design system primitives (Button, Card,
// Dialog, etc.) instead of duplicating them per-app.
export {};
